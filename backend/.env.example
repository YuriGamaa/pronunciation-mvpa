# Backend configuration (example)
# Where the frontend is running (CORS)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# (Optional) Port for uvicorn if you want a single command
PORT=8000
